# Created by generate-pxe-menu! Do NOT edit unless you know what you are doing! 
# Keep those comment "MENU DEFAULT" and "MENU HIDE"! Do NOT remove them.
# Note!!! If "serial" directive exists, it must be the first directive
default vesamenu.c32
timeout 300
prompt 0
noescape 1
MENU MARGIN 5
 MENU BACKGROUND TfNodeBackupTool800x600.png
# Set the color for unselected menu item and timout message
 MENU COLOR UNSEL 7;32;41 #c0000090 #00000000
 MENU COLOR TIMEOUT_MSG 7;32;41 #c0000090 #00000000
 MENU COLOR TIMEOUT 7;32;41 #c0000090 #00000000
 MENU COLOR HELP 7;32;41 #c0000090 #00000000

# MENU MASTER PASSWD

say **********************************************************************
say omniflow.cc | Backup and Wipe Threefold Node Tool
say This Tool uses sources from Clonezilla as base!
say Made by github:fl0wm0ti0n
say base sources:
say NCHC Free Software Labs, Taiwan.
say clonezilla.org, clonezilla.nchc.org.tw
say THIS SOFTWARE COMES WITH ABSOLUTELY NO WARRANTY! USE AT YOUR OWN RISK!
say **********************************************************************

# Allow client to edit the parameters
ALLOWOPTIONS 1

# simple menu title
MENU TITLE omniflow.cc | Backup and Wipe Threefold Node Tool

label Quick backup the seed with random naming of the backupfile
  MENU DEFAULT
  # MENU HIDE*
  MENU LABEL Quick backup the seed with random naming of the backupfile (VGA 800x600)
  # MENU PASSWD
  kernel /live/vmlinuz
    append initrd=/live/initrd1.img boot=live union=overley username=user hostname=nodebackuptool edd=on nolocales ocs_live_run="/scripts/BackupNode.sh -q -s" ocs_live_extra_param="" ocs_live_batch="no" ocs_lang="" locales=en_US.UTF-8 keyboard-layouts=de noprompt mode_option=800x600 toram=filesystem.squashfs  
  TEXT HELP
    Quick backup the seed with random naming (date-time) of the backupfile
  ENDTEXT

label Backup the seed manually naming after the provided nodeid
  MENU DEFAULT
  # MENU HIDE*
  MENU LABEL Quick backup the seed with random naming of the backupfile (VGA 1280x960)
  # MENU PASSWD
  kernel /live/vmlinuz
    append initrd=/live/initrd1.img boot=live union=overley username=user hostname=nodebackuptool edd=on nolocales ocs_live_run="/scripts/BackupNode.sh -q -s" ocs_live_extra_param="" ocs_live_batch="no" ocs_lang="" locales=en_US.UTF-8 keyboard-layouts=de noprompt mode_option=1280x960 toram=filesystem.squashfs  
  TEXT HELP
    Quick backup the seed with random naming (date-time) of the backupfile
  ENDTEXT

label mount CIFS volume and do it manually
  MENU DEFAULT
  # MENU HIDE*
  MENU LABEL mount CIFS volume and do it manually (VGA 800x600)
  # MENU PASSWD
  kernel /live/vmlinuz
    append initrd=/live/initrd1.img boot=live union=overley username=user hostname=nodebackuptool edd=on nolocales ocs_live_run="/scripts/WipeNode.sh" ocs_live_extra_param="" ocs_live_batch="no" ocs_lang="" locales=en_US.UTF-8 keyboard-layouts=de noprompt mode_option=800x600 toram=filesystem.squashfs  
  TEXT HELP
    set up dhcp then you will be asked to enter CIFS parameters and can work on your own in shell
  ENDTEXT

MENU BEGIN Memtest & FreeDOS
# Note! *.bin is specially purpose for syslinux, 
# Do NOT use memtest.bin, use memtest instead of memtest.bin
label memtest
  # MENU DEFAULT
  # MENU HIDE
  MENU LABEL Memory test using Memtest86+
  # MENU PASSWD
  kernel /live/memtest
  TEXT HELP
  Run memory test using Memtest86+
  ENDTEXT

label FreeDOS
  # MENU DEFAULT
  # MENU HIDE
  MENU LABEL FreeDOS
  # MENU PASSWD
  kernel memdisk
  append initrd=/live/freedos.img
  TEXT HELP
  Run FreeDOS
  ENDTEXT

MENU END

MENU END
